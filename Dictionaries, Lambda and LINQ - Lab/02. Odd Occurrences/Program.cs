using System;
using System.Collections.Generic;
using System.Linq;

class Program
{
    static void Main()
    {
        //var words = Console.ReadLine().ToLower().Split(' ');

        //var thisWord = new Dictionary<string, int>();

        //foreach (var word in words)
        //{
        //    if (!thisWord.ContainsKey(word))
        //    {
        //        thisWord[word] = 0;
        //    }

        //    thisWord[word]++;
        //}

        //var result = new List<string>();

        //foreach (var article in thisWord)
        //{
        //    if (article.Value % 2 != 0)
        //    {
        //        result.Add(article.Key);
        //    }
        //}

        //Console.WriteLine(string.Join(", ", result));

        
    }
}

